"use strict";
// Q12 Answer:
let namess = ['Ramzan', 'Subhan', 'Sarim', 'Asif', 'Nasrullah'];
for (let nom = 0; nom < namess.length; nom++) {
    let nim = namess[nom];
    console.log(`Hello ${nim}, would you like to learn some TypeScript today?`);
}
;
